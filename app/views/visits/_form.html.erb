<%= form_for(@visit) do |f| %>
  <% if @visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% @visit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :store_id %><br />
    <%= f.collection_select :store_id, Store.all, :id, :store_name %>
  </div>
  <div class="field">
    <%= f.label :date_visited %><br />
    <%= f.date_select :date_visited %>
  </div>
  <div class="field">
    <%= f.label :spent %><br />
    <%= f.text_field :spent %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
